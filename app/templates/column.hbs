{{#bs-button type="button" clicked="remove" }}<i class="fa fa-times-circle-o"></i>{{/bs-button}}

{{model.name}}

{{#each model.cards itemController='card'}}
  {{#unless model.archived}}
    {{view 'card'}}
  {{/unless}}
{{/each}}
